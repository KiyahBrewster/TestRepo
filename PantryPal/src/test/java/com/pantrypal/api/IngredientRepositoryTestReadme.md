**Purpose:** Verifies that the `IngredientRepository` loads seeded data correctly and supports basic repository behavior.

**Responsibilities**

Confirms that IngredientRepository.findAll() returns data from the seed file (data.sql).

Validates that the seed data includes an Ingredient named "egg".

Ensures the repository is wired correctly and works with JPA/Hibernate.